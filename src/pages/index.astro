---
import Layout from '../layouts/Layout.astro';
import getLastPosts from '../lib/getLastPosts';
import LastPosts from '../components/LastPosts.astro';

const posts = await getLastPosts()

console.group(posts.map(o=>o.categories))
---

<Layout title="No Screen without DOOM">
	<LastPosts/>
</Layout>

